= Section 2 — Deploy application with short-lived credentials
:toc: left
:toclevels: 3
:icons: font
:experimental:
:idprefix:
:idseparator: -

[NOTE]
====
In NIST SP 800-207 terms, this section moves from *verifying* the ZTA stack (Section 1) to *using* it under load. **AAP** acts as the *Policy Enforcement Point (PEP)* that gates every deployment step. **OPA** (the *Policy Decision Point*) evaluates group membership before secrets are issued. **Vault** generates *short-lived, per-run* database credentials instead of long-lived passwords. **Arista** enforces *micro-segmentation* so that even valid credentials are only usable from the correct network segment.

**Why that matters:** An attacker who compromises one layer — network, identity, or secrets — still faces enforcement at every other layer. The deployment pipeline is not a single trust decision; it is a **chain of independent verifications** that must all pass.
====

== Overview

Deploy the **Global Telemetry Platform** using Vault dynamic database credentials. Experience OPA **deny** for the wrong user, then **allow** for `app-deployers`. Observe micro-segmentation (Arista) and credential TTL.

[IMPORTANT]
.Why this section exists
====
Deploying an app is where **identity (who)**, **policy (may they)**, **secrets (with what credential)**, and **network (on which path)** meet. In Zero Trust you **do not** hand every engineer a long-lived DB password or open the database to the whole VLAN. This pipeline shows **policy before secrets** and **least-privilege network paths** aligned with short-lived credentials—so compromise of one layer does not instantly mean full data access.
====


== Exercise 2.1
[%collapsible]
====         
[TIP]
.Separation of concerns in templates
====
Each step has a single job: **decide** (OPA), **issue** (Vault), **enforce path** (Arista), **configure app**. That mirrors ZTA's idea that **no single giant playbook** should skip gates—each stage can fail closed, be audited, and be replaced without collapsing the whole story.
====

As the ztuser, login to the automation platform and navigate to Automation Execution -> Templates and click on Create template to create and Workflow Template.

Create the workflow with the following details:

[rows="2,2,3",options="header"]
|===
| Name |Deploy Application Pipeline
| Organization | Default
| Inventory | ZTA Lab Inventory
|===

Next, click on 'Create workflow job template', this will take you to the Workflow Visualizer. We will now create a workflow to isolate each single job.

Click on the 'Add step' button to begin. On the new menu on the right, select 'Job Template' for Node type, and then use the drop down list to select the 'Check DB policy' template. Click Next, and then Finish.

This template is the first to execute and it will check with OPA to ensure the job runs in policy. Next, we need to add to the workflow. Right cick on the kebab menu on the template we just added. <> PICUTRE

Select 'add Step and link', choose the 'Create DB Credetnial' template this time, and under Status select 'Run on success'. This ensures that the job will only run if the previous step was successful. Click Next and Finish. 

Add the next two templates the same way, 'Configure DB Access list', and 'Deploy Application'

Your workflow should look like this:

image::

Lastly, click 'Save'. Click on the workflow name, and on the template details select 'Team Access'. Click on 'Assign Teams', select all the teams and click next. Now we need to allocate roles for the teams, you can select 'WorkflowJobTemplate Approve' and 'WorkflowJobTemplate Execute' and click next followed by review and finish.

The roles will now be allocated, and you can log out of the platform. 
====

[NOTE]
.AAP workflows as auditable trust chains
====
**Ansible Automation Platform's workflow visualizer** makes the trust chain *visible*: each node is a discrete policy gate or operational step. If any node fails, the chain stops — there is no "skip to deploy." This is not just convenience; it is **auditability by design**. Every run records which gates passed, which failed, who launched it, and when. In a post-incident review, that trail is the difference between "we think it was authorised" and "here is proof."
====

[TIP]
.Order of operations = trust boundaries
====
**Check DB Access Policy runs first** on purpose: if the human (via AAP) is not entitled to database credentials, the workflow **never touches Vault**. That is *defence in depth*—the secret manager is not a substitute for authorisation. In breach terms, an attacker who tricks someone into clicking "deploy" still hits a **PDP (OPA)** before any **secret issuance**.
====

== Exercise 2.2
[%collapsible]
====  
. Log into AAP as `neteng`. Notice you have no permissions. You are just a member of the Organization.

. Navigate to the IDM tab and login with the admin details.
. Search for neteng on active users. Select the account and click on User Groups.We can see they are part of the 'ipausers' group so they have access but not much else. 
. click on 'Add' so we can add them to the Infrastructure group. Search for Infrastructure and click on the '>' to allocate the group to the account and click 'Add'.

image::



Navigate back to the controller. Log out and log back in as the neteng user. 

[NOTE]
.AAP + IdM: real-time RBAC without local accounts
====
When you changed `neteng`'s group membership in IdM, AAP picked up that change on next login — no local account editing, no CSV import, no manual team assignment. This is **centralised identity lifecycle** in action: the identity provider (IdM) is the single source of truth, and AAP's LDAP integration ensures that **access follows identity changes in near real-time**. In Zero Trust, stale permissions are almost as dangerous as no permissions — this integration closes that gap.
====

Navigate to Templates and execute the 'Deploy Application Pipeline' workflow by clicking on the rocket.

We can see the job failed! Click on the first template to review.
image::

We can see OPA has stepped in and **DENIED** the job. The workflow stops and no futher actions can take place.

[source,yaml]
----
_ansible_verbose_always: true
evaluated_to: false
assertion: opa_result.allow | default(false)
msg: |
  ACCESS DENIED by OPA policy.
  User 'neteng' is not authorised to request database credentials.
  Reason: unknown
  Conditions: {}
_ansible_no_log: false
changed: false
----

Now logout and lets login in with the 'appdev' user. Navigate to the same template and trigger it. Notice this user has been approved to trigger this automation. Click on the Check DB policy now to see the details.

[source]
----
TASK [Display OPA decision] ****************************************************
ok: [central] => {
    "msg": "OPA Database Access Decision:\n User: appdev\n Groups (IdM): team-applications, app-deployers, ipausers\n Database: ztaapp\n Decision: ALLOWED\n Reason: all conditions met — credential issuance approved\n \n"
}
Monday 13 April 2026 19:58:52 +0000 (0:00:00.042) 0:00:02.446 **********

TASK [Enforce OPA decision] ****************************************************
ok: [central] => {
    "changed": false,
    "msg": "OPA policy check passed — proceeding with credential issuance"
}
----

[TIP]
.Identity-driven access
====
**IdM groups** are attributes the PDP understands. Changing group membership is how the organisation **grants or revokes** deployment authority without editing playbooks. That is **policy + identity lifecycle** working together—important when people change roles or leave.
====

Go back to the workflow and it should have completed successfully. Navigate to the 'Terminal' tab, and from central lets check the application:


[source]
----
curl -s http://app.zta.lab:8081/health | python3 -m json.tool
----

We should have a reply similar to:

[source]
----
{
    "database": true,
    "status": "healthy",
    "ts": 1776246070
}
----

We can see the application is healthy with its new time limited credentials.

Navigate to the 'Global Telemetry' tab and view the application is working.
The time limited credential should last for 5 mins. Run the curl again and view the change.

In the workflow, click on the "Create DB Access List" template, and we can see the following:

[source]
----
TASK [Display ACL configuration] ***********************************************
ok: [ceos2] => {
"msg": "Network micro-segmentation applied on ceos2:\n ACL: ZTA-APP-TO-DB\n PERMIT: 10.20.0.10 → 10.30.0.10:5432\n DENY: any → 10.30.0.10:5432\n\nOnly the app container (10.20.0.10) can reach the\ndatabase via the Arista cEOS switch fabric.\n"
}
----

We can verify this on the switch if you open the 'Terminal' tab and ssh into the switch.

[source:bash]
----
[rhel@central ~]$ ssh -p 2002 admin@central.zta.lab
----
NOTE: Switch credentials: admin/admin

[source:bash]
----
ceos2#show ip access-lists ZTA-APP-TO-DB
IP Access List ZTA-APP-TO-DB
        10 permit tcp host 10.20.0.10 host 10.30.0.10 eq 5432
        20 permit tcp host 10.30.0.10 eq 5432 host 10.20.0.10
        100 deny ip any host 10.30.0.10
----

Exit the switch, and ssh into the DB.

[source:bash]
----
[rhel@central ~]$ ssh -p 2022 rhel@192.168.1.11
----

NOTE: Authentication details admin/ansible123!

View the dynamic usernames that have been created:

[source:bash]
----
[rhel@db ~]$ sudo -u postgres psql -c "\du" | grep v-userpass
 v-userpass-ztaapp-s-DWnNLePRQqnufQmAhJ47-1776246021 | Password valid until 2026-04-15 09:45:26+00               
| {}
----

[TIP]
.Micro-segmentation after authorisation
====
Even with valid DB credentials, **network policy** still limits *where* connections may originate. The Arista step models **segmentation** that is common in ZTA: secrets prove *who the app is*, ACLs constrain *which paths are valid*. An attacker with stolen creds from a random laptop should still fail if the fabric only permits the app tier IP.
====

Once 5mins have gone past, we can now check the status of the application. Navigate back to 'Terminal' and run the same curl. 


[source]
----
curl -s http://app.zta.lab:8081/health | python3 -m json.tool

{
    "database": false,
    "status": "degraded",
    "ts": 1776246423
}
----

The application is no longer healthy. The time limited credentials have now expired. Refresh the 'Global Telemetry Platform' web page and you will see a database connection error. 
==== 

[TIP]
.The 503 is Zero Trust working as intended
====
A healthy app that *stops working* when credentials expire is proof that **time-bound access** is enforced end-to-end. In a traditional environment, database passwords live in config files for years. Here, the credential **self-destructs** — and the app's health check honestly reflects that. This is the operational cost of Zero Trust, and the benefit: **blast radius is bounded by time**, not by someone remembering to rotate a password.
====

[TIP]
.What is this about ?
====
The unauthorised user **never sees** a database password. Zero Trust is not only about encryption—it is about **shrinking who can cause sensitive actions**. Deny-by-default policy turns "I have network reachability" into "I still cannot obtain credentials."
====

[TIP]
.Ephemeral credentials change operations
====
TTL expiry is a feature, not a bug: it forces **rotation**, **re-authorisation**, or **deliberate renewal** instead of silent, multi-year database passwords. In ZTA, **standing access** to data is the exception and must be justified; here the app must be **re-fed** credentials on a schedule or on demand—exactly what `Rotate DB Credentials` (extended) practices.
====



== Hands-on: Write the OPA policy check

_ZTA concept: *Policy Decision Point integration* -- querying OPA from a playbook turns an external policy engine into a mandatory trust gate that Ansible enforces inline._

This exercise gives you a **skeleton playbook** with the IdM group lookup already done. Your job is to write the three missing tasks that query OPA, display the result, and enforce the decision.

NOTE: Login as ztauser on the Ansible Automation Platform

[IMPORTANT]
.What to do - 
====
. Open `section2/playbooks/check-db-policy-student.yml` view it in Gitea.
. The playbook has three commented-out tasks marked `YOUR CODE STARTS HERE`. Each has hints and `FILL_IN` placeholders.
. Uncomment each task and replace the `FILL_IN` placeholders with the correct values.
. In AAP, navigate to the Automation Execution -> Projects, and sync the ZTA Workshop Project.
. In AAP, create a new job template called **Check DB Policy** pointing to your skeleton playbook. Use the same inventory (ZTA Lab Inventory), Project (ZTA Workshop) and Playbook (section2/playbooks/check-db-policy-student.yml)
. Launch as `neteng` -- expect OPA **DENIED**.
. Launch as `appdev` -- expect OPA **ALLOWED**.
====

[cols="1,3",options="header"]
|===
| Task | What you write
| 1. Query OPA | `uri` module POST to `{{ opa_url }}/v1/data/zta/db_access/decision` with an `input` body containing `user`, `user_groups`, `target_database`, and `requested_permissions`.
| 2. Display decision | `debug` module showing the user, groups, decision (`allow`), and reason from `opa_decision.json.result`.
| 3. Enforce | `assert` module that fails the play if `opa_decision.json.result.allow` is false.
|===


== Hands-on: Fix the broken deployment

_ZTA concept: *Security misconfiguration is an attack surface* -- a single wrong `mode`, a missing `no_log`, or a hardcoded credential can undermine an otherwise sound ZTA pipeline._

This exercise gives you a **deliberately broken** deployment playbook with four security and reliability bugs.

[IMPORTANT]
.What to do
====
. In AAP, create a job template called **Deploy Application (Broken)** pointing to `section2/playbooks/deploy-application-broken.yml`. Same inventory, credentials, and EE as the working version.
. Duplicate the 'Deploy Application Pipeline' workflow template and replace the last template with your new broken template in the workflow visualizer.
. Launch as `appdev`
. Run the workflow to get valid Vault credentials, then launch this template.
. It will **fail**. Read the error output and the playbook source to find all four bugs:

[cols="1,3",options="header"]
!===
! Bug ! ZTA violation
! 1 ! Credentials are exposed in the AAP job log
! 2 ! The env file is world-readable on the container
! 3 ! A hardcoded password overrides the Vault dynamic credential
! 4 ! The health check URL is wrong so deployment is never validated
!===

. Fix all four bugs in the playbook and re-run until the deployment succeeds.
. Compare your fixes against the working `deploy-application.yml`.
====

[TIP]
.Defensive coding in ZTA
====
These are **real-world** misconfigurations that break Zero Trust guarantees. Bug 1 (`no_log` missing) means ephemeral credentials end up in logs -- defeating their short-lived nature. Bug 2 (`mode 0644`) violates least privilege. Bug 3 (hardcoded password in systemd) is the classic "someone put a static cred in for debugging and forgot." Bug 4 (wrong port) means the deployment appears to succeed but nobody verified the app actually works. ZTA demands that **every link in the chain** is correct, not just the identity and policy layers.
====

You can now proceed to the next section! 


== Corrected Playbook - Write the OPA policy check:
[scope:yaml]
----
    - name: Query OPA for database access decision
      uri:
        url: "{{ opa_url }}/v1/data/zta/db_access/decision"
        method: POST
        body_format: json
        body:
          input:
            user: "{{ requesting_user }}"
            user_groups: "{{ requesting_groups }}"
            target_database: "{{ db_name }}"
            requested_permissions: ["SELECT", "INSERT", "UPDATE"]
      register: opa_decision

    - name: Display OPA decision
      debug:
        msg: |
          OPA Database Access Decision:
            User:     {{ requesting_user }}
            Groups:   {{ requesting_groups | join(', ') }}
            Database: {{ db_name }}
            Decision: {{ 'ALLOWED' if opa_decision.json.result.allow else 'DENIED' }}
            Reason:   {{ opa_decision.json.result.reason }}

    - name: Enforce OPA decision
      assert:
        that:
          - opa_decision.json.result.allow | default(false)
        fail_msg: |
          ACCESS DENIED by OPA policy.
          User '{{ requesting_user }}' is not authorised to request database credentials.
          Reason: {{ opa_decision.json.result.reason | default('unknown') }}
        success_msg: "OPA policy check passed — proceeding with credential issuance"
----
